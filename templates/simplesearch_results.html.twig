{% extends 'partials/base.html.twig' %}

{% block content %}
<div id="primary" class="search-results">
    <form class="search-form" action="{{ base_url_relative }}/search" method="get">
		<input class="search-input" type="search" placeholder="Поиск..." id="search-input" autocomplete="off" value="{{ query }}" autofocus onfocus="this.value = this.value;" name="query">
		<button class="search-submit" type="submit">Search</button>
	</form>
    {% for page in search_results %}
        {% include 'partials/simplesearch_item.html.twig' with {'page':page} %}
    {% endfor %}
    {% if search_results.count == 0 %}
    <p>К сожалению, по запросу «{{ query }}» ничего не найдено.</p>
    <p>Поищите что-нибудь ещё, например, <a href="{{ base_url_relative }}/search?query=Super+Metroid">Super Metroid</a>.</p>
    {% endif %}
</div>
{% endblock %}

{% block body %}
<body class="{{ browser.getbrowser }} open">
{% endblock %}

{% block header %}
    <a href="/" id="backtoindex">
        <h1>{{ site.title }}</h1>
    </a>

    <input type="checkbox" id="nav-trigger" class="nav-trigger" />
    <label for="nav-trigger" id="open-button">
        <div class="spinner diagonal part-1"></div>
        <div class="spinner horizontal"></div>
        <div class="spinner diagonal part-2"></div>
    </label>

    <div class="menu-wrap" id="morphSearch">
        <div class="hamburger-insides">
            <nav id="about-links">
                <ul>
                {% for p in pages.children %}{% if p.visible %}
                    <li><a href="{{ p.rawRoute() }}" class="no-u">{{ p.title }}</a></li>
                {% endif %}{% endfor %}
                </ul>
            </nav>
        </div>
    </div>
{% endblock %}
